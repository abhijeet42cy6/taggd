GEMINI_API_KEY="your api key"

# Local test admin (bootstrap runs only when users table is empty)
AUTH_BOOTSTRAP_EMAIL="change all these on server"
AUTH_BOOTSTRAP_PASSWORD="change all these on setrver"

# Composio integration (Microsoft Teams / Outlook calendar)
# Keep these server-side only; do NOT expose as frontend secrets.
COMPOSIO_API_KEY="your api key"
COMPOSIO_WEBHOOK_SECRET=
COMPOSIO_BASE_URL=https://backend.composio.dev
COMPOSIO_ENV=prod
